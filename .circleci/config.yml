# PHP CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-php/ for more details
version: 2
jobs:
  build:
    docker:
      # Image to use as build runner.
      - image: debian:jessie
    steps:
      - checkout
      - run:
          name: "Exec 1"
          command: mkdir /dir1 && mv sleep.sh /dir1 && /dir1/sleep.sh

